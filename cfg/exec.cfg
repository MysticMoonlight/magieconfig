// setup aliases
exec "user/reset.cfg"
exec "empty.cfg"
exec "binds/core.cfg"
exec "help/core.cfg"

// apply user settings
exec "settings.cfg"
exec "settings_class.cfg"
exec "custom.cfg"

// finalize
exec "finalize.cfg"
